Step 1 — Download the two files from our chat. In yesterday's message where I delivered the patch set, grab page.svelte and page.server.ts (or just open them to copy their contents). The naming rule so they don't get crossed again: page.svelte starts with <script lang="ts"> — that's the UI component. page.server.ts starts with import { query } — that's the data loader.
Step 2 — Fix +page.svelte on the PR branch. Go to the repo → switch branch to DelProtoPrize-patch-4 (the PR #3 branch) → navigate to dashboard/src/routes/+page.svelte → click the pencil (Edit) → select all, delete → paste the full contents of my page.svelte → "Commit changes" directly to DelProtoPrize-patch-4 (not a new branch). Suggested commit message: fix: restore Svelte component to +page.svelte.
Step 3 — Fix +page.server.ts on the same branch. Still on DelProtoPrize-patch-4 → navigate to dashboard/src/routes/+page.server.ts → Edit → select all, delete → paste the full contents of my page.server.ts → commit to the same branch. Message: fix: FC movement metrics, prev HHI, single-snapshot guard in load.
Step 4 — Update the branch. Open PR #3 → scroll to the bottom near the merge box → click "Update branch". This pulls this morning's main into the branch — your CI fixes (pg, Plotly) and the merged InsightRail — without which dashboard-tests would keep failing no matter what.
Step 5 — Wait for checks, then merge. The checks re-run automatically after steps 2–4. All four should go green (etl-tests and python-lint already were). When they're green, merge PR #3.
That's it. Nothing gets deleted, no new PR, no new branch — the existing PR just gets its contents corrected and its base refreshed. If any check is still red after step 5, don't merge; tell me and I'll read the actual failure log before suggesting anything else.
